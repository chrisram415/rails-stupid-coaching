<%# <%= @question %>

<h1>Stupid Coaching</h1>

<div class="card">
  <div class="card-header">
    Question
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p>
          <% if @question.blank? %>
          😳
        <% else %>
          🥸 <%= @question %>
        <% end %>
      </p>
      <footer class="blockquote-footer">You</footer>
    </blockquote>
  </div>
</div>



<div class="card">
  <div class="card-header">
    The Answer
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p>
          😡 <%= @answer %>
      </p>
      <footer class="blockquote-footer">Your coach</footer>
    </blockquote>
  </div>
</div>

<%# <%= link_to "Ask a new question", ask_path %>

<button type="button" class="btn btn-dark btn-lg"><%= link_to "Ask more Questions", ask_path %></button>
